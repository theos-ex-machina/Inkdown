# Inkdown

Literally just minimal obsidian with inking.

Only the features I need to take notes for school.

## Run it

```powershell
npm install
npm run tauri:dev
```

## Build an installer

```powershell
npm run tauri:build
```

Installer ends up in `src-tauri/target/release/bundle/`.
